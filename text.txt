return Stack(
  children: [SizedBox(
        width: screen.width,
        height: screen.height * 2/3,
        child: FittedBox(
          fit: BoxFit.cover,
          child: SizedBox(
            width:_controller!.value.previewSize!.height,
            height: _controller!.value.previewSize!.width,
            child: CameraPreview(_controller!),
          ),
        ),
      ),Column(mainAxisSize: MainAxisSize.min,children:[const Icon(
                              Icons.flash_on,
                              color: Colors.white,
                              size: 40,
                            ),const SizedBox(height: 15),
                    GestureDetector(
                      onTap: () {;
                      },
                      child: const Icon(
                        Icons.change_circle,
                        color: Colors.white,
                        size: 40,
                      ),
                    ),])]);
